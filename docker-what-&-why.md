# Docker What & Why

Docker is a platform for deploying applications. It runs the same across all operating systems.

Two key parts:
* Container
* Image

Container is an isolated environment on a computer. It has it's own files, folders, memory, networking, separated from the main OS.

Image is a read-only file that contains a set of instructions on how to create a container.
Container is a runnable instance of an image. 
